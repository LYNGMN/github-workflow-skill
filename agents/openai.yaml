interface:
  display_name: "Managing GitHub Workflows"
  short_description: "Safe GitHub Issues, Pull Requests, review, and Merge"
  default_prompt: "Use $managing-github-workflows to complete this GitHub workflow with clear scope, verification, privacy, and approval boundaries; match progress and status labels to the user's language and write specialized terms in Korean first with the English original in parentheses when writing Korean."

policy:
  allow_implicit_invocation: true
